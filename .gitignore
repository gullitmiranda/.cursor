# This repo stores shareable Cursor config in `.cursor/`.
# Prefer a denylist: keep versionable files visible by default and only ignore
# machine-generated/runtime artifacts.

# macOS
.DS_Store

# Cursor runtime/state (often machine/user specific and/or large)
projects/
subagents/
plugins/

# Cursor installation artifacts (very large, never version)
extensions/
snapshots/
ai-tracking/
interpreter-notebooks/
skills-cursor/

# Editor config (keep useful settings versioned)
.vscode/
!.vscode/
!.vscode/settings.json
!.vscode/extensions.json
.vscode/mise-tools/

# Local-only misc state
blocklist

# Local-only config/state (may contain machine IDs or secrets)
mcp.json
argv.json
cli-config.json
ide_state.json
unified_repo_list.json

# If these ever get created inside the repo, ignore them too
**/terminals/
**/agent-tools/
**/agent-notes/

# Generic logs
*.log
